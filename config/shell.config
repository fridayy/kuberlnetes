%% -*- mode: erlang -*-
[
  {kuberlnetes, [
  ]},
  %% kernel config
  {kernel,
    %% logging
    [
      {logger_level, info},
      {logger,
        [{handler, default, logger_std_h,
          #{formatter => {logger_formatter, #{single_line => true}}}}
        ]}]}
].
